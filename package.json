{
  "name": "easygoal-packages",
  "private": true,
  "description": "Monorepo de packages compartilhados do ecossistema Easy Goal",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "type-check": "pnpm -r run type-check",
    "build:ui": "pnpm --filter @easygoal/ui run build",
    "build:auth": "pnpm --filter @easygoal/auth run build"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
